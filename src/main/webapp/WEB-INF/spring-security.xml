<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-4.0.xsd">

    <global-method-security pre-post-annotations="enabled"/>

    <http pattern="/login" security="none"/>
    <http pattern="/signup" security="none"/>

    <http pattern="/**" entry-point-ref="restAuthenticationEntryPoint" create-session="stateless">
        <csrf disabled="true"/>
        <custom-filter before="FORM_LOGIN_FILTER" ref="jwtAuthenticationFilter"/>
    </http>

    <beans:bean id="jwtAuthenticationFilter" class="com.globallogic.store.security.jwt.JwtAuthenticationFilter">
        <beans:property name="authenticationManager" ref="authenticationManager"/>
        <beans:property name="authenticationSuccessHandler" ref="jwtAuthenticationSuccessHandler"/>
    </beans:bean>

    <beans:bean id="restAuthenticationEntryPoint" class="com.globallogic.store.security.jwt.RestAuthenticationEntryPoint"/>

    <beans:bean id="jwtAuthenticationSuccessHandler" class="com.globallogic.store.security.jwt.JwtAuthenticationSuccessHandler"/>

    <beans:bean id="jwtAuthenticationProvider" class="com.globallogic.store.security.jwt.JwtAuthenticationProvider"/>

    <authentication-manager alias="authenticationManager">
        <authentication-provider ref="jwtAuthenticationProvider"/>
    </authentication-manager>

    <!-- <http use-expressions="true">
         &lt;!&ndash; interceptors &ndash;&gt;
         <intercept-url method="DELETE" pattern="/users" access="hasAuthority('ADMIN')"/>
         <intercept-url pattern="/user" access="hasAuthority('ADMIN')"/>
         <intercept-url pattern="/order" access="hasAuthority('ADMIN')"/>
         <intercept-url pattern="/cart" access="hasAuthority('CUSTOMER')"/>
         <intercept-url pattern="/account" access="isAuthenticated()"/>

         &lt;!&ndash; access denied page &ndash;&gt;
         <access-denied-handler
                 error-page="/403"/>

         <form-login
                 login-page="/login"
                 login-processing-url="/j_spring_security_check"
                 username-parameter="username"
                 password-parameter="password"
                 default-target-url="/home"
                 authentication-failure-url="/login?error"/>

         <logout
                 logout-url="/j_spring_security_logout"
                 logout-success-url="/home"/>

         <csrf disabled="true"/>
     </http>

     <authentication-manager alias="authenticationManager">
         <authentication-provider user-service-ref="loginUserService"/>
     </authentication-manager>-->

</beans:beans>